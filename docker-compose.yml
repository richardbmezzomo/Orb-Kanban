services:
  postgres:
    image: postgres:17
    container_name: postgres_db
    environment:
      POSTGRES_DB: orb
      POSTGRES_USER: docker 
      POSTGRES_PASSWORD: docker
    ports:
      - '5432:5432'
